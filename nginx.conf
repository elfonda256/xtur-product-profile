server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript application/rss+xml application/atom+xml image/svg+xml;

    # Security Headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Single Page Application / Clean URLs
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Static Assets Caching
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
    }

    # File Exports Download Headers (Matches Vercel configuration)
    location ~* ^/exports/.*\.pdf$ {
        add_header Content-Type "application/pdf";
        add_header Content-Disposition "attachment";
    }

    location ~* ^/exports/.*\.pptx$ {
        add_header Content-Type "application/vnd.openxmlformats-officedocument.presentationml.presentation";
        add_header Content-Disposition "attachment";
    }

    # Custom Error Pages
    error_page 404 /index.html;
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
